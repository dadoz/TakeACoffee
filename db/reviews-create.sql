CREATE TABLE `takeacoffee`.`reviews` (
  `id` INT NOT NULL,
  `comment` VARCHAR(45) NULL,
  `status` VARCHAR(45) NOT NULL,
  `timestamp` VARCHAR(45) NOT NULL,
  `username_id` INT NULL,
  PRIMARY KEY (`id`));
